import { stopDataRetentionTimer, startDataRetentionTimer, triggerDataRetentionCheck } from "./DeviceDataRetention";

/*
describe("triggerDataRetentionCheck", () => {
  test("assure correct type", () => {
    const data = "string";
    const parsed = getTagKeys(data);
    expect(parsed).toBeInstanceOf(Array);
  });
});

describe("startDataRetentionTimer", () => {
  test("assure correct type", () => {
    const data = "string";
    const parsed = getTagKeys(data);
    expect(parsed).toBeInstanceOf(Array);
  });
});


describe("stopDataRetentionTimer", () => {
  test("assure correct type", () => {
    const data = "string";
    const parsed = getTagKeys(data);
    expect(parsed).toBeInstanceOf(Array);
  });
});
*/
