/**
 * Types of buttons. Each one will have specific theme color and size.
 */
export enum EButton {
  default = "default",
  primary = "primary",
  danger = "danger",
  icon = "icon",
  warning = "warning",
  danger_outline = "danger_outline",
}
