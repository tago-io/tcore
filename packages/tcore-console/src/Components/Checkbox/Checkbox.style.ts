import styled from "styled-components";

/**
 * Main checkbox style.
 */
export const Container = styled.label`
  display: flex;
  align-items: center;
  cursor: pointer;

  > div {
    font-weight: normal;
    margin-left: 5px;
  }
`;

export default Container;
