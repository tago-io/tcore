import styled from "styled-components";

const Header = styled.div`
  height: auto;

  .right-side-options {
    position: absolute;
    top: 10px;
    right: 0px;
    display: flex;
    flex-direction: row;
    align-items: flex-end;
  }
`;

export { Header };
